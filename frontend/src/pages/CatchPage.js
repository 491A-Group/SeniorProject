import './HomePage.css';

export default function CatchPage({changePage, imageString}) {
    //the main return to display the home page or main feed
    return (
      <div>
        <p>{imageString}</p>
      </div>
    );
  }
  